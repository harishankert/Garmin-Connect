name: Build and Push Multi-Architecture Docker Image (Latest Build)

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - auth-fix
    paths:
      - 'src/garmin_grafana/garmin_fetch.py'
      - 'src/garmin_grafana/influxdb_exporter.py'
      - 'src/garmin_grafana/garmin_bulk_importer.py'
      - 'src/garmin_grafana/fit_activity_importer.py'
      - 'src/garmin_grafana/__init__.py'
      - 'compose-example.yml'
      - 'pyproject.toml'
      - 'Dockerfile'
      - '.python-version'
      - 'uv.lock'
      - '.github/workflows/prod.push.yml'

env:
  IMAGE_NAME: garmin-grafana
  IMAGE_TAG: latest
  IMAGE_REPO: harishankert

jobs:
  build_and_push:
    name: Build Multi-Arch Docker Image and Push to Docker Hub & GHCR
    if: ${{ github.repository == 'harishankert/Garmin-Connect' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner}}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU for Multi-Architecture Support
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Resolve Image Tag From Branch
        run: |
          if [ "${GITHUB_REF_NAME}" = "main" ]; then
            echo "IMAGE_TAG=latest" >> "$GITHUB_ENV"
          else
            SAFE_TAG="${GITHUB_REF_NAME//\//-}"
            echo "IMAGE_TAG=${SAFE_TAG}" >> "$GITHUB_ENV"
          fi

      - name: Build Multi-Arch Image (linux/amd64, linux/arm64) and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}

        
